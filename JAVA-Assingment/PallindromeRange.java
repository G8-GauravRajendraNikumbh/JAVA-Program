import java.io.DataInputStream;
import java.io.IOException;
class PallindromeRange
{
	public static void main(String args[])
	{
		int str=0,end=0,i=0;
		boolean flag = false;
		DataInputStream d = new DataInputStream(System.in);
		System.out.println("program for printing all pallindrome numbers in given range");
		try
		{
			System.out.print("Enter the any starting No = ");
			str=Integer.parseInt(d.readLine());
			System.out.print("Enter the any Ending No = ");
			end=Integer.parseInt(d.readLine());
		}
		while(str<=end)
		{
			pallindrome
		}
	}

}